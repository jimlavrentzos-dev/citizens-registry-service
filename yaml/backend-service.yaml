apiVersion: v1
kind: Service
metadata:
  name: citizens-backend
  namespace: citizens
  labels:
    app: citizens-backend
spec:
  type: NodePort   
  selector:
    app: citizens-backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      nodePort: 30080  
      name: http
